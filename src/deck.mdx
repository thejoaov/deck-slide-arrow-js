import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { nightOwl, github, dracula, vsDark } from "@code-surfer/themes";

export const theme = vsDark;

## ES6 Arrow Functions 
# Versus 
## "Normal" Function Syntax 

---
## Normal Function syntax
---

<CodeSurfer>

```js 1[1:23] 
function normalFunction(arg1, arg2){
  const someValue = arg1+arg2;
  return someValue;
}
```
```js 1[25:35] 
function normalFunction(arg1, arg2){
  const someValue = arg1+arg2;
  return someValue;
}
```
```js 2[3:18] 
function normalFunction(arg1, arg2){
  const someValue = arg1+arg2;
  return someValue;
}
```
```js 2[21:30] 
function normalFunction(arg1, arg2){
  const someValue = arg1+arg2;
  return someValue;
}
```
```js 3 
function normalFunction(arg1, arg2){
  const someValue = arg1+arg2;
  return someValue;
}
```
</CodeSurfer>

---
## Arrow Function Syntax (ES6)
---

<CodeSurfer>

```js 1
const arrowFunction = (arg1, arg2) => arg1 + arg2;
```
```js 1[1:21]
const arrowFunction = (arg1, arg2) => arg1 + arg2;
```
```js 1[23:34]
const arrowFunction = (arg1, arg2) => arg1 + arg2;
```
```js 1[36:50]
const arrowFunction = (arg1, arg2) => arg1 + arg2;
```
</CodeSurfer>

---

<CodeSurferColumns themes={[nightOwl, github]}>

<Step>

```js
const arrowFunction = (a, b) => a + b;
```

```js
function normalFunction(a, b){
  return a + b;
}
```

</Step>

<Step>

```js
const loadProducts = async () =>{
  const response = await api.get('/products');
  setProducts(response.data)
}

const arrowFunction = (a, b) => a + b;
```

```js
function normalFunction(a, b){
  return a + b;
}

async function loadProducts(){
  const response = await api.get('/products');
  setProducts(response.data)
}
```

</Step>

</CodeSurferColumns>

---

GitHub, Twitter, Instagram:
[@thejoaov](https://thejoaov.com)
